{
  "name": "hackdavis",
  "version": "1.0.0",
  "description": "",
  "main": "content.js",
  "scripts": {
    "setup": "node setup.cjs",
    "build": "node setup.cjs && esbuild content.js --bundle --format=esm --outfile=content.bundle.js",
    "watch": "node setup.cjs && esbuild content.js --bundle --format=esm --outfile=content.bundle.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "onnxruntime-web": "^1.26.0"
  }
}
